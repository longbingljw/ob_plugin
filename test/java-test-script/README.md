# 多语言分词测试工具

一个简单易用的交互式多语言文本分词测试工具，支持日文、韩文、泰文三种语言。

## 支持语言

| 语言 | 引擎 | 说明 |
|------|------|------|
| 🇯🇵 日文 | Apache Lucene Kuromoji | 日文文本分词 |
| 🇰🇷 韩文 | Apache Lucene Nori | 专业韩文分词 |
| 🇹🇭 泰文 | Apache Lucene ThaiAnalyzer | 泰文文本分词 |

## 快速开始

### 启动工具

**方式1: 直接执行脚本**
```bash
# 首次使用需要设置执行权限
chmod +x test_multilang.sh

# 然后直接运行
./test_multilang.sh
```

**方式2: 使用bash执行**
```bash
bash test_multilang.sh
```

### 使用流程
1. **选择语言模式** - 输入 `1`、`2` 或 `3`
2. **输入文本** - 直接输入要分词的文本
3. **查看结果** - 立即显示分词结果和token数量
4. **切换语言** - 输入 `switch` 切换到其他语言
5. **退出程序** - 输入 `quit` 退出

## 使用示例

### 启动界面
```
🌍 Starting Multi-Language Segmenter Test...
Select language mode:
  1. Japanese (日本語) 🇯🇵
  2. Korean (한국어) 🇰🇷
  3. Thai (ภาษาไทย) 🇹🇭
  help - Show examples
  quit - Exit

Select mode (1-3): 1
```

### 日文分词示例
```
JP_Input: 東京都渋谷区でコンピューターを勉強しています
Result: [東京, 都, 渋谷, 区, コンピュータ, 勉強]
Token count: 6
```

### 韩文分词示例
```
KO_Input: 데이터베이스
Result: [데이터, 베이스]
Token count: 2
```

### 泰文分词示例
```
TH_Input: ฐานข้อมูล
Result: [ฐาน, ข้อมูล]
Token count: 2
```

## 命令说明

| 命令 | 功能 |
|------|------|
| `1` / `2` / `3` | 选择语言模式 |
| `switch` | 切换到其他语言 |
| `help` | 显示使用说明和示例 |
| `quit` / `stop` | 退出程序 |

## 批处理脚本

支持批量处理文本文件的三种语言分词脚本：

### 支持的脚本

| 脚本 | 语言 | 输出目录 |
|------|------|----------|
| `batch_japanese.sh` | 🇯🇵 日文 | `java/jp_results/` |
| `batch_korean.sh` | 🇰🇷 韩文 | `java/ko_results/` |
| `batch_thai.sh` | 🇹🇭 泰文 | `java/th_results/` |

### 使用方法

```bash
# 设置执行权限（首次使用）
chmod +x batch_*.sh

# 处理文件（支持相对路径和绝对路径）
./batch_japanese.sh input_file.txt
./batch_korean.sh /full/path/to/korean_text.txt
./batch_thai.sh ../data/thai_text.txt
```

### 输出格式

- 每行文本分词结果用逗号分隔
- 输出文件带时间戳：`jp_YYYYMMDD_HHMMSS.txt`
- 显示处理统计：总行数、总词元数

### 注意事项

- 输入文件每行一个句子
- 空行保持不变
- 结果自动保存到对应语言的results目录

## 📁 文件结构

```
├── test_multilang.sh          # 交互式测试脚本
├── batch_japanese.sh          # 日语批处理脚本
├── batch_korean.sh            # 韩语批处理脚本
├── batch_thai.sh              # 泰语批处理脚本
├── java/
│   ├── *.class               # 编译后的Java字节码
│   └── lib/                  # Lucene JAR包
│       ├── lucene-core-8.11.2.jar
│       ├── lucene-analyzers-kuromoji-8.11.2.jar
│       ├── lucene-analyzers-nori-8.11.2.jar
│       └── lucene-analyzers-common-8.11.2.jar
│   ├── jp_results/           # 日语分词结果
│   ├── ko_results/           # 韩语分词结果
│   └── th_results/           # 泰语分词结果
└── README.md                 # 本文档
```

